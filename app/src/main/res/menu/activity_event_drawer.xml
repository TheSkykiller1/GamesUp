<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_follow"
            android:icon="@drawable/ic_follow"
            android:title="@string/followedgame" />
        <item
            android:id="@+id/nav_follow1"
            android:icon="@drawable/ic_follow"
            android:title="@string/liste" />
    </group>

    <item android:title="@string/filter_title">
        <menu>
            <item
                android:id="@+id/filter_ac"
                android:icon="@drawable/ic_menu_send"
                android:title="@string/filter_asc_date" />
            <item
                android:id="@+id/filter_dc"
                android:icon="@drawable/ic_menu_send"
                android:title="@string/filter_desc_date" />
        </menu>
    </item>

</menu>
