/***Avec string.xml */
        /**ListView des différents panneau contenant les informations*/

        rowGames_items = new ArrayList<Event_list_games>();

        /**Liste stocker dans string.xml*/
        /*member_names = getResources().getStringArray(R.array.Member_names);
        //profile_pics = getResources().obtainTypedArray(R.array.profile_pics);
        statues = getResources().getStringArray(R.array.statues);
        contactType = getResources().getStringArray(R.array.contactType);*/

        //games_pics = getResources().obtainTypedArray(R.array.Games_Pics);
        games_names = getResources().getStringArray(R.array.Games_Names);
        games_plateform = getResources().getStringArray(R.array.Games_Plateform);
        games_releasedate = getResources().getStringArray(R.array.Games_ReleaseDate);

        for (int i = 0; i < games_names.length; i++) {
            Event_list_games item = new Event_list_games(i+1,
                    games_names[i], /*games_pics.getResourceId(i, -1),*/ games_plateform[i],
                    games_releasedate[i]);
            rowGames_items.add(item);
        }

        mylistview = (ListView) findViewById(R.id.list);
        Event_list_adapter adapter = new Event_list_adapter(this, rowGames_items);
        mylistview.setAdapter(adapter);

        //mylistview.setOnItemClickListener(this);
		
		
		
		
		
		
		
		
		
 // TypedArray games_pics;
    String[] games_names;
    String[] games_plateform;
    String[] games_releasedate;
	
	    /*String[] member_names;
    TypedArray profile_pics;
    String[] statues;
    String[] contactType;*/

  